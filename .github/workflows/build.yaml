name: Build project
on:
  push:
    branches: ['**']
jobs:
  build:
    name: Emulate build
    runs-on: ubuntu-latest
    steps:
      - run: echo $EVENT_PAYLOAD | jq -C '.'
        env:
          EVENT_PAYLOAD: ${{ toJSON(github.event) }}
